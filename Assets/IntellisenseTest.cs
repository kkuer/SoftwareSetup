using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IntellisenseTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float krisVariable = 1f;
        krisVariable += 1;
        krisVariable -= 1;
    }
}
